interface:
  display_name: "NekoLib Repository File Inventory"
  short_description: "Inspect files, docs, skills, and topology"
  default_prompt: "Use $repo-file-inventory to inspect NekoLib file distribution, documentation or skill topology, Git state, or a compact Markdown tree."

policy:
  allow_implicit_invocation: true
